<UserControl x:Class="PasswordManager.Pages.SettingsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PasswordManager.Pages"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:Background="White">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Orientation="Vertical" HorizontalAlignment="Left">
            <StackPanel Orientation="Horizontal">
                <Label Style="{DynamicResource H1}">General</Label>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <Label>Backup path:</Label>
                <Label x:Name="BackupPath" Content="undefined" />
                <Button Style="{DynamicResource Button_Link}" Content="Edit" Click="BackupPathEdit_Click" />
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <Label>Authenticaion code:</Label>
                <Label x:Name="AuthenticationCode">undefined</Label>
                <Button x:Name="AddCodeButton" Style="{DynamicResource Button_Link}" Content="Add" Click="AddAuthCode_Click" />
                <Button x:Name="RemoveCodeButton" Style="{DynamicResource Button_Link}" Content="Remove" Click="RemoveAuthCode_Click" />
            </StackPanel>
            
            <StackPanel Orientation="Horizontal">
                <Label Style="{DynamicResource H1}">Appearance</Label>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <Label>Dark theme:</Label>
                <Button x:Name="DarkThemeButton" Style="{DynamicResource Button_Link}" Click="DarkTheme_Click" Content="On" />
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</UserControl>
