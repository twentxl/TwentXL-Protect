<UserControl x:Class="PasswordManager.Components.Modal_AddData"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PasswordManager.Components"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40" />
            <RowDefinition Height="80" />
            <RowDefinition Height="80" />
            <RowDefinition Height="80" />
            <RowDefinition Height="80" />
            <RowDefinition Height="40" />
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Style="{DynamicResource H1}">Add password</Label>
        <StackPanel Grid.Row="1" Orientation="Vertical" VerticalAlignment="Center">
            <TextBox x:Name="Title" Tag="title*" Height="40"/>
        </StackPanel>
        <StackPanel Grid.Row="2" Orientation="Vertical" VerticalAlignment="Center">
            <TextBox x:Name="Login" Tag="login" Height="40"/>
        </StackPanel>
        <StackPanel Grid.Row="3" Orientation="Vertical" VerticalAlignment="Center">
            <TextBox x:Name="Password" Tag="password*" Height="40"/>
            <Button HorizontalAlignment="Right" Style="{DynamicResource Button_Link}" Margin="0,0,20,0" Click="GeneratePassword_Click">Generate password</Button>
        </StackPanel>
        <StackPanel Grid.Row="4" Orientation="Vertical" VerticalAlignment="Center">
            <TextBox x:Name="Additional" Tag="additionality" Height="40"/>
        </StackPanel>
        <Grid Grid.Row="5" HorizontalAlignment="Right">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100" />
                <ColumnDefinition Width="100" />
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Style="{DynamicResource Button}" Margin="0,0,20,0" Click="Add_Click">Add</Button>
            <Button Grid.Column="1" Style="{DynamicResource ButtonDanger}" Click="Cancel_Click">Cancel</Button>
        </Grid>
    </Grid>
</UserControl>
