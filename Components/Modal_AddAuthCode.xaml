<UserControl x:Class="PasswordManager.Components.Modal_AddAuthCode"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PasswordManager.Components"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="40" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <Label Style="{DynamicResource H1}" Content="Enter authentication code" />
            <TextBox x:Name="Code" Tag="Enter code" Height="40"/>
        </StackPanel>

        <Grid Grid.Row="1" HorizontalAlignment="Right">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100" />
                <ColumnDefinition Width="100" />
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Style="{DynamicResource Button}" Margin="0,0,20,0" Click="Add_Click">Add</Button>
            <Button Grid.Column="1" Style="{DynamicResource ButtonDanger}" Click="Cancel_Click">Cancel</Button>
        </Grid>
    </Grid>
</UserControl>
