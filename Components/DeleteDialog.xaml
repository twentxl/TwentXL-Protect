<UserControl x:Class="PasswordManager.Components.DeleteDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PasswordManager.Components"
             mc:Ignorable="d" 
             d:DesignHeight="211.912" d:DesignWidth="401.107" d:Background="White">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="40" />
        </Grid.RowDefinitions>

        <Label Style="{DynamicResource H1}" Grid.Row="0" Content="Are you sure you want to delete the password?" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="16" Margin="0,0,0,20"/>
        <Grid Grid.Row="1" HorizontalAlignment="Right">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100" />
                <ColumnDefinition Width="100" />
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Style="{DynamicResource Button}" Margin="0,0,20,0" Click="Delete_Click">Delete</Button>
            <Button Grid.Column="1" Style="{DynamicResource ButtonDanger}" Click="Cancel_Click">Cancel</Button>
        </Grid>
    </Grid>
</UserControl>
